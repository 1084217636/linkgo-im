# CMD

`cmd/` 目录存放所有可执行程序入口。每个子目录对应一个独立进程，负责把配置、依赖注入和服务启动串起来。

## 子目录

- `gateway/`：接入层，承接 HTTP 和 WebSocket。
- `logic/`：逻辑层，承接 gRPC、路由、持久化和会话序列分配。
- `transfer/`：预留给后续消息投递或 MQ 异步链路扩展。

## 设计原则

- `cmd` 层不堆业务逻辑，只做启动和组装。
- 真正的业务实现放在 `internal/`。
