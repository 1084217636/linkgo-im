# Example only. Do not commit a real Secret; create linkgo-im-secret through
# External Secrets, Sealed Secrets, or your CI secret store before deployment.
apiVersion: v1
kind: Secret
metadata:
  name: linkgo-im-secret
  namespace: linkgo-im
type: Opaque
stringData:
  REDIS_PASSWORD: replace-me
  JWT_SECRET: replace-with-at-least-32-random-bytes
  DB_DSN: "linkgo:replace-me@tcp(mysql-primary.example.internal:3306)/linkgo_im?charset=utf8mb4&parseTime=true&loc=Local"
