apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../namespace.yaml
  - configmap.yaml
  - ../rbac.yaml
  - ../gateway.yaml
  - ../logic.yaml
  - ../transfer.yaml
  - ../hpa.yaml
  - ../networkpolicy.yaml

# This overlay deploys application workloads only. Redis, MySQL, Kafka and
# Etcd are shared HA services managed outside this application release.
